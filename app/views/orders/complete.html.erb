Kartınızı okutunuz
<%= form_tag complete_orders_path, data: {method: :post} do %>
    <%= text_field_tag :card, params[:card] %>
    <% if params[:admin] %>
        <%= hidden_field_tag :admin, params[:admin] %>
        <%= select_tag :account_id, options_for_select(Account.pluck(:name, :id)) %>
    <% end %>
    <%= submit_tag 'Öde' %>
    <%= link_to 'Vazgeç', root_path %>
<% end %>

<%= link_to 'Yönetici Giriş', complete_orders_path(admin: 1), data: {method: :post} %>