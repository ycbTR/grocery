<div>
  <h1 class="text-center">Z Raporu</h1>
  <table class="table table-bordered table-striped">
    <thead>
    <tr>
      <th colspan="3" class="text-right">
        <%= form_tag zreport_reports_path, method: :get, class: 'form-inline' do %>
            <%= text_field_tag :start, params[:start], placeholder: 'Başlangıç zamanı 2019-01-01 13:00', class: "form-control" %>
            <%= text_field_tag :end, params[:end], placeholder: 'Bitiş zamanı 2019-01-01 00:00', class: "form-control" %>
            <%= submit_tag 'Getir', class: 'btn btn-default' %>
            <%= link_to 'Bugün', zreport_reports_path, class: 'btn btn-default' %>
        <% end %>
      </th>
    </tr>
    <tr>
      <th colspan="3"><%= l @start_time %> - <%= l @end_time %> saatleri arası</th>
    </tr>

    <tr>
      <th>Ürün</th>
      <th>Adet</th>
      <th>Tutar</th>
    </tr>
    </thead>

    <tbody>
    <% @product_report.each do |name, h| %>
        <tr>
          <td>
            <%= name %>
          </td>
          <td>
            <%= h[:count] %>
          </td>
          <td>
            ₺<%= h[:total] %>
          </td>
        </tr>
    <% end %>

    <% if @product_report.empty? %>
        <tr>
          <td colspan="3" class="text-center">Sipariş bulunamadı</td>
        </tr>
    <% end %>
    <tr>
      <td colspan="2" class="text-right">
        Sipariş Sayısı
      </td>
      <td>
        <%= @orders.count %>
      </td>
    </tr>

    <tr>
      <td colspan="2" class="text-right">
        Toplam Sipariş Tutarı
      </td>
      <td>
        <b>₺<%= @total.to_f %></b>
      </td>
    </tr>
    <tr>
      <td colspan="2" class="text-right">
        Yüklenen Bakiye
      </td>
      <td>
        <b>₺<%= @balance_added.to_f %></b>
      </td>
    </tr>
    </tbody>
  </table>

  <%= link_to url_for({:only_path => false, format: :xls}.merge(params.permit!)), target: '_blank' do %>
      Excel'e aktar
  <% end %>

</div>